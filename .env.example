# Optional — AI help (falls back to built-in explanations if unset)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional — email delivery via Resend
RESEND_API_KEY=
EMAIL_FROM=WillGuide <onboarding@resend.dev>
